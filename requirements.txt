# Runtime dependencies. Deliberately small -- the whole app is stdlib sqlite3
# and hand-written SQL, and a smaller surface is a smaller thing to keep working
# on a machine that is not this one.
fastapi>=0.110
uvicorn[standard]>=0.27
jinja2>=3.1
python-multipart>=0.0.9

# Unofficial Garmin Connect client. Pinned loosely because Garmin changes its
# internal endpoints without notice and the fixes arrive as point releases --
# see providers/garmin.py for why every call through it is wrapped.
garminconnect>=0.2.25
